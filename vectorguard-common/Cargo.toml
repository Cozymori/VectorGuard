[package]
name = "vectorguard-common"
version = "0.1.0"
edition = "2024"

# no_std support: shared with the eBPF crate
[features]
default = ["user"]
user = ["serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
