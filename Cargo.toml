[workspace]
members = [
    "vectorguard",
    "vectorguard-ebpf",
    "vectorguard-common",
]
# vectorguard-ebpf targets bpfel-unknown-none only — excluded from host builds
default-members = [
    "vectorguard",
    "vectorguard-common",
]
resolver = "2"
